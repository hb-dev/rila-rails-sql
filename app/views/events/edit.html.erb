<% content_for :topic_nav do %>
	<%= link_to 'Löschen', @event, class: "btn btn-danger pull-right", method: :delete, data: { confirm: 'Veranstaltung und alle dazugehörigen Läufe und Anmeldungen löschen?' } %>
<% end %>

<%= render "shared/header" %>

<%= render 'form' %>

